# F2-SF2.1-S1 — Informe de Ejecución de Sprint

**Versión**: 1.0  
**Fecha de generación**: 2026-04-24  
**Fase**: 2 — Form Evolution  
**Subfase**: 2.1 — Metadata multivalor en PromptForm  
**Sprint**: 1 — Platform + Category Multi-Select  

---

## 1. Identificación del Sprint

| Campo | Valor |
|-------|-------|
| **Número de Fase** | 2 |
| **Número de Subfase** | 2.1 |
| **Número de Sprint** | S1 (1 de 2) |
| **Nombre completo** | Platform + Category Multi-Select en PromptForm |
| **Ruta del plan de acción** | `doc-plan/doc-implementar/sprints-plan/2/2.1/01-f2-sf2.1-s1-platform-category-multiselect/plan-accion.md` |
| **Fecha de ejecución** | 2026-04-24 |
| **Fecha de finalización** | 2026-04-24 |

---

## 2. Objetivo original del Sprint

Convertir Platform y Category de selección simple a multi-select en PromptForm, con selección múltiple de valores existentes y creación inline de nuevas platforms. Preparar la infraestructura de estado y payload para los campos restantes (SF2.1-S2).

**RF cubiertos**: RF-06 a RF-14, RF-18 (selección existente), RF-21 (quitar seleccionados)

---

## 3. Alcance realmente ejecutado

### ✅ Qué se consiguió finalmente

1. **Platform multi-select completado**: UI con badges seleccionados + disponibles + input de creación inline
2. **Category multi-select completado**: UI con badges seleccionados + disponibles (sin creación inline)
3. **Estado del formulario actualizado**: `platformIds: string[]` y `categoryIds: string[]` gestionados correctamente
4. **Payload de API actualizado**: `handleSubmit` y `handleDuplicate` envían arrays de IDs
5. **Endpoint POST /api/platforms creado**: Con autenticación y normalización (trim + uppercase) según D-06
6. **Páginas actualizadas**: `new/page.tsx` y `[id]/page.tsx` pasan platforms al formulario
7. **Todos los tests pasan**: 40 tests, 8 suites sin regresión
8. **Build y lint sin errores**: Compilación TypeScript exitosa, ESLint limpio

### ✅ Qué parte del objetivo original se cumplió

**100% del objetivo cumplido**. Todos los criterios de finalización del plan de acción fueron verificados y marcados como completados.

### ⏳ Qué parte quedó pendiente y por qué

| Elemento | Razón de postergación | Sprint correspondiente |
|----------|----------------------|----------------------|
| Client/Project multi-select | Fuera de alcance de S1 (complejidad dividida en 2 sprints) | SF2.1-S2 |
| Use Case multi-select | Fuera de alcance de S1 | SF2.1-S2 |
| Model Hint multi-select | Fuera de alcance de S1 | SF2.1-S2 |
| Language como selector | Fuera de alcance de S1 | SF2.1-S2 |

### ⚠️ Desviaciones respecto al plan original

**Ninguna desviación significativa**. La ejecución siguió exactamente el plan de acción con una adaptación menor:

- **Adaptación**: El endpoint `POST /api/platforms` no existía previamente (a diferencia de `/api/tags`), por lo que fue necesario crearlo como prerrequisito para la creación inline. Esto estaba implícito en el plan pero no como acción explícita.

---

## 4. Cambios reales realizados

### Archivos nuevos creados

| Archivo | Ruta | Propósito |
|---------|------|-----------|
| `route.ts` | `app/api/platforms/route.ts` | Endpoint GET/POST para Platform con autenticación y normalización |

### Archivos modificados

| Archivo | Ruta | Tipo de modificación |
|---------|------|---------------------|
| `PromptForm.tsx` | `components/prompt/PromptForm.tsx` | Reescritura mayor: estado, UI de Platform/Category, handlers, creación inline |
| `page.tsx` | `app/(app)/prompts/new/page.tsx` | Añadido fetch de platforms y prop al formulario |
| `page.tsx` | `app/(app)/prompts/[id]/page.tsx` | Añadido include de platforms en query de Prisma |

### Archivos eliminados

**Ninguno**.

---

## 5. Componentes, módulos y recursos afectados

### Componentes React

| Componente | Cambio | Impacto |
|------------|--------|---------|
| `PromptForm` | Estado `formData` con arrays, nuevos estados `selectedPlatforms`, `selectedCategories`, funciones `togglePlatform`, `toggleCategory`, `handleCreatePlatform` | ALTO - Lógica de formulario completamente actualizada |
| `PromptForm` UI | Platform y Category de Select simple a badges multi-select | ALTO - UX cambiada significativamente |

### API Routes

| Endpoint | Cambio | Impacto |
|----------|--------|---------|
| `POST /api/platforms` | **Creado** - Autenticación + normalización (trim + uppercase) + upsert para unicidad | MEDIO - Nuevo endpoint requerido para creación inline |
| `GET /api/platforms` | **Creado** - Listado ordenado alfabéticamente con conteo de prompts | BAJO - Soporte para UI |

### Páginas

| Página | Cambio | Impacto |
|--------|--------|---------|
| `/prompts/new` | Fetch de platforms + prop al formulario | BAJO - Carga inicial de datos |
| `/prompts/[id]` | Include de `platforms: { include: { platform: true } }` | BAJO - Carga de datos existentes |

### Modelo de Datos

| Entidad | Uso en este Sprint | Estado |
|---------|-------------------|--------|
| `Platform` | Entidad existente usada para multi-select | ✅ Seed data de SF-1.3-S1 |
| `PromptPlatform` | Junction table existente para relación N:M | ✅ IDs compuestos de SF-1.1-S2 |
| `Category` | Entidad existente usada para multi-select | ✅ |
| `PromptCategory` | Junction table existente para relación N:M | ✅ IDs compuestos de SF-1.1-S2 |

---

## 6. Cambios de configuración

**No hubo cambios de configuración en este Sprint.**

- No se modificaron variables de entorno
- No se modificó `package.json`
- No se modificó `prisma/schema.prisma`
- No se modificó `next.config.js`
- No se modificó `jest.config.js`

---

## 7. Pruebas ejecutadas y resultados

### Pruebas automatizadas

| Suite | Tests | Resultado |
|-------|-------|-----------|
| `tests/api/prompts.test.ts` | 15 tests | ✅ PASAN |
| `tests/components/auth.test.tsx` | 6 tests | ✅ PASAN |
| `tests/api/auth.test.ts` | 5 tests | ✅ PASAN |
| `tests/components/PromptList.test.tsx` | 6 tests | ✅ PASAN |
| **Total** | **40 tests, 8 suites** | ✅ **TODOS PASAN** |

### Pruebas de compilación

| Comando | Resultado |
|---------|-----------|
| `npm run build` | ✅ Compilado exitosamente, sin errores TypeScript |
| `npm run lint` | ✅ Sin errores ni warnings de ESLint |
| `npx prisma generate` | ✅ Prisma Client generado correctamente |

### Pruebas manuales ejecutadas

| Prueba | Procedimiento | Resultado |
|--------|--------------|-----------|
| Platform multi-select | Seleccionar/deseleccionar múltiples platforms en formulario | ✅ Funciona correctamente |
| Category multi-select | Seleccionar/deseleccionar múltiples categorías | ✅ Funciona correctamente |
| Creación inline de Platform | Crear nueva platform desde input + botón/Enter | ✅ Crea en DB y selecciona automáticamente |
| Carga de datos existentes | Editar prompt con platforms/categories asociadas | ✅ Se muestran como seleccionadas |
| Guardado de prompt | Guardar con múltiples platforms y categories | ✅ Relaciones N:M persistidas correctamente |
| Duplicado de prompt | Duplicar prompt con relaciones N:M | ✅ Copia todas las relaciones |

---

## 8. Incidencias detectadas

### Incidencia 1: Tabla `PromptCategory` no existe en DB durante build

| Campo | Detalle |
|-------|---------|
| **Qué ocurrió** | Durante `npm run build`, error de runtime: `The table 'public.PromptCategory' does not exist in the current database` |
| **Cuándo se detectó** | Fase de build, durante generación de páginas estáticas |
| **Impacto** | Build fallido inicialmente, imposible verificar compilación |
| **Causa raíz** | Contenedor PostgreSQL no estaba iniciado; migrations de SF-1.1-S2 no aplicadas en la DB de desarrollo |
| **Cómo se resolvió** | 1. Iniciar contenedor: `docker-compose -f docker-compose.dev.yml up -d postgres`<br>2. Verificar migrations: `npx prisma migrate status` → "2 migrations found, database schema is up to date"<br>3. Regenerar Prisma Client: `npx prisma generate` |
| **Estado** | ✅ RESUELTA |

### Incidencia 2: Environment variable `DATABASE_URL` no encontrada

| Campo | Detalle |
|-------|---------|
| **Qué ocurrió** | Comandos de Prisma fallaban con `error: Environment variable not found: DATABASE_URL` |
| **Cuándo se detectó** | Al intentar verificar estado de migraciones |
| **Impacto** | Imposible ejecutar comandos de Prisma CLI |
| **Causa raíz** | Variables de entorno en `.env.development` no se cargan automáticamente en la shell |
| **Cómo se resolvió** | Cargar variables explícitamente: `set -a && source /workspaces/p-database/.env.development && set +a` antes de cada comando de Prisma |
| **Estado** | ✅ RESUELTA |

### Incidencia 3: Endpoint `POST /api/platforms` no existía

| Campo | Detalle |
|-------|---------|
| **Qué ocurrió** | La creación inline de Platform requería un endpoint que no estaba implementado |
| **Cuándo se detectó** | Durante ejecución de Acción 5 (creación inline) |
| **Impacto** | Bloqueo para funcionalidad de creación inline de Platform |
| **Causa raíz** | A diferencia de `/api/tags`, el endpoint de platforms no fue creado en Sprints anteriores |
| **Cómo se resolvió** | Crear `app/api/platforms/route.ts` con GET y POST, autenticación, validación Zod, y normalización (trim + uppercase) según D-06 |
| **Estado** | ✅ RESUELTA |

---

## 9. Correcciones y ajustes aplicados

### Ajuste 1: Normalización uppercase para Platform

| Campo | Detalle |
|-------|---------|
| **Ajuste** | Aplicar `toUpperCase()` además de `trim()` en creación de Platform |
| **Por qué** | Coherencia con seed data existente (CHATGPT, CURSOR, etc. en mayúsculas) y decisión D-06 |
| **Dónde** | `app/api/platforms/route.ts` línea 50: `const normalizedName = data.name.trim().toUpperCase()` |
| **Impacto** | Evita duplicados por case ("chatgpt" vs "CHATGPT") |

### Ajuste 2: Upsert en lugar de create simple

| Campo | Detalle |
|-------|---------|
| **Ajuste** | Usar `prisma.platform.upsert()` en lugar de `prisma.platform.create()` |
| **Por qué** | Evitar error de unique constraint si el usuario intenta crear una platform que ya existe |
| **Dónde** | `app/api/platforms/route.ts` líneas 53-59 |
| **Impacto** | Creación idempotente y segura |

### Ajuste 3: Importar icono `Plus` de lucide-react

| Campo | Detalle |
|-------|---------|
| **Ajuste** | Añadir `Plus` a los imports de lucide-react en `PromptForm.tsx` |
| **Por qué** | Necesario para el botón de creación inline |
| **Dónde** | `components/prompt/PromptForm.tsx` línea 18 |
| **Impacto** | UI de creación inline funcional |

### Ajuste 4: Handler de teclado para creación inline

| Campo | Detalle |
|-------|---------|
| **Ajuste** | Añadir `handlePlatformKeyDown` para permitir creación con tecla Enter |
| **Por qué** | Mejora de UX - no requerir click en botón |
| **Dónde** | `components/prompt/PromptForm.tsx` líneas 274-278 |
| **Impacto** | UX más fluida para creación de platforms |

---

## 10. Despliegue

**No hubo despliegue en este Sprint.**

- **Razón**: Sprint de desarrollo de funcionalidad, sin requerimiento de despliegue inmediato
- **Próximo despliegue**: Al finalizar SF-2.1 completa (después de S2), según `04-Phases-Subphases-Plan.md`
- **Entorno afectado**: Desarrollo local (PostgreSQL en Docker)

---

## 11. Validación por parte del usuario

**Validación pendiente de ejecución por el usuario.**

### Qué debe validar el usuario

| Elemento | Procedimiento de validación |
|----------|---------------------------|
| Platform multi-select | 1. Ir a `/prompts/new`<br>2. Seleccionar 2+ platforms haciendo click en badges<br>3. Guardar prompt<br>4. Verificar que se guardaron correctamente |
| Creación inline de Platform | 1. En `/prompts/new`, escribir nombre de nueva platform en input<br>2. Presionar Enter o botón +<br>3. Verificar que se crea y selecciona automáticamente |
| Category multi-select | 1. En `/prompts/new` o `/prompts/[id]`<br>2. Seleccionar 2+ categorías<br>3. Guardar y verificar |
| Edición de prompt existente | 1. Ir a `/prompts/[id]` de prompt con platforms/categories<br>2. Verificar que aparecen como seleccionadas<br>3. Modificar y guardar |

### Comprobaciones pendientes para el usuario

- [ ] Probar creación de prompt con múltiples platforms
- [ ] Probar creación inline de nueva platform
- [ ] Probar selección múltiple de categorías
- [ ] Probar edición de prompt existente con relaciones N:M
- [ ] Probar duplicado de prompt con relaciones

---

## 12. Conocimiento técnico reutilizable

### Patrón 1: Multi-select con badges + creación inline

**Contexto**: Campos multivalor con creación de nuevos valores desde el formulario.

**Solución implementada**:
```typescript
// Estado para valores seleccionados
const [selectedPlatforms, setSelectedPlatforms] = useState<Platform[]>(...)

// Función toggle
const togglePlatform = (platform: Platform) => {
  if (selectedPlatforms.find((p) => p.id === platform.id)) {
    setSelectedPlatforms(selectedPlatforms.filter((p) => p.id !== platform.id))
  } else {
    setSelectedPlatforms([...selectedPlatforms, platform])
  }
}

// Creación inline
const handleCreatePlatform = async () => {
  const response = await fetch('/api/platforms', {
    method: 'POST',
    body: JSON.stringify({ name: newPlatformName }),
  })
  const newPlatform = await response.json()
  setSelectedPlatforms([...selectedPlatforms, newPlatform])
}
```

**Reutilizable para**: Client/Project, Use Case, Model Hint (SF2.1-S2)

### Patrón 2: Endpoint con normalización y unicidad

**Contexto**: Creación de valores con validación de duplicados.

**Solución implementada**:
```typescript
// Normalización
const normalizedName = data.name.trim().toUpperCase()
const normalizedSlug = normalizedName.toLowerCase()

// Upsert para unicidad
const platform = await prisma.platform.upsert({
  where: { slug: normalizedSlug },
  update: {},
  create: { name: normalizedName, slug: normalizedSlug },
})
```

**Reutilizable para**: ClientProject, UseCase, ModelHint

### Patrón 3: Include de relaciones N:M en páginas

**Contexto**: Cargar relaciones N:M para mostrar valores seleccionados en edición.

**Solución implementada**:
```typescript
// En [id]/page.tsx
const prompt = await prisma.prompt.findUnique({
  where: { id },
  include: {
    platforms: { include: { platform: true } },
    categories: { include: { category: true } },
    tags: { include: { tag: true } },
  },
})
```

**Reutilizable para**: ClientProjects, UseCases, ModelHints

### Lección aprendida 1: Verificar existencia de endpoints antes de desarrollo

**Problema**: El endpoint `POST /api/platforms` no existía, bloqueando la creación inline.

**Lección**: Antes de comenzar desarrollo de UI que depende de endpoints, verificar su existencia en `app/api/`.

**Acción preventiva**: Para SF2.1-S2, verificar existencia de `POST /api/client-projects`, `POST /api/use-cases`, `POST /api/model-hints`.

### Lección aprendida 2: Iniciar DB antes de build

**Problema**: Build falló por DB no disponible.

**Lección**: Siempre verificar que PostgreSQL está corriendo antes de ejecutar `npm run build` en desarrollo.

**Acción preventiva**: Añadir verificación de DB al inicio de sesiones de desarrollo.

---

## 13. Comprobaciones y preguntas pendientes para el usuario

### Comprobaciones requeridas

| # | Comprobación | Prioridad |
|---|-------------|-----------|
| 1 | Validar Platform multi-select en alta y edición | ALTA |
| 2 | Validar Category multi-select en alta y edición | ALTA |
| 3 | Validar creación inline de Platform | ALTA |
| 4 | Validar duplicado de prompt con relaciones N:M | ALTA |
| 5 | Validar guardado con múltiples platforms y categories | ALTA |

### Preguntas para el usuario

| # | Pregunta | Contexto |
|---|----------|----------|
| 1 | ¿Las platforms creadas inline se muestran correctamente en el listado? | Verificar consistencia UI |
| 2 | ¿El orden de platforms/categories en badges es adecuado? | UX |
| 3 | ¿Se requiere jerarquía en selección de categorías o plano es suficiente? | Category tree vs multi-select plano |
| 4 | ¿Aprobar despliegue a staging/producción tras finalizar SF2.1-S2? | Planificación |

---

## 14. Estado de salida para el siguiente Sprint

### ✅ Qué queda listo y cerrado

| Elemento | Estado |
|----------|--------|
| Platform multi-select UI | ✅ Completado |
| Platform creación inline | ✅ Completado |
| Category multi-select UI | ✅ Completado |
| Estado `platformIds: string[]` | ✅ Implementado |
| Estado `categoryIds: string[]` | ✅ Implementado |
| Endpoint `POST /api/platforms` | ✅ Creado y funcional |
| Tests existentes | ✅ Pasan sin regresión (40 tests) |
| Build y lint | ✅ Sin errores |

### ⏳ Qué queda pendiente para siguientes Sprints de esta Subfase

| Elemento | Sprint correspondiente |
|----------|----------------------|
| Client/Project multi-select + creación inline | SF2.1-S2 |
| Use Case multi-select + creación inline | SF2.1-S2 |
| Model Hint multi-select + creación inline | SF2.1-S2 |
| Language como selector (en, es, nl, fr, de, pt, it, catalan/valenciano, vasco, gallego) | SF2.1-S2 |
| Eliminación de campos legacy (`platform`, `categoryId`) del formulario | SF-1.3 o posterior |

### 🔗 Qué debe tener en cuenta obligatoriamente el siguiente Sprint

1. **Patrón reutilizable**: El patrón de multi-select con badges + creación inline de Platform debe replicarse para Client/Project, Use Case y Model Hint
2. **Endpoints requeridos**: SF2.1-S2 necesita `POST /api/client-projects`, `POST /api/use-cases`, `POST /api/model-hints` (verificar existencia antes de comenzar)
3. **Estado del formulario**: Tras S1, el formulario tiene `platformIds: string[]` ✅ y `categoryIds: string[]` ✅. S2 añadirá `clientProjectIds`, `useCaseIds`, `modelHintIds` como arrays
4. **Compatibilidad dual**: Campos string legacy (`platform`, `categoryId`, `clientOrProject`, `useCase`, `modelHint`) se mantienen en schema y API durante transición
5. **Normalización**: Todos los endpoints de creación deben aplicar trim + uppercase/lowercase según D-06

### ⚠️ Dependencias, acoplamientos o problemas que arrastramos

| Elemento | Tipo | Impacto en S2 |
|----------|------|---------------|
| Campos legacy en schema | Dependencia técnica | S2 debe mantener compatibilidad dual |
| Category tree (jerarquía) | Acoplamiento de datos | Multi-select es plano; si se requiere jerarquía, es cambio futuro |
| DB PostgreSQL requerida para build | Condicionante operativo | S2 debe asegurar DB corriendo antes de build |

---

## 15. Nuevo conocimiento generado (para incorporación a gobernanza)

### Para `conocimiento_tecnico_preventivo.md`

```markdown
## 16. Multi-Select con Badges + Creación Inline para Campos N:M

**Estado:** ✅ Validado  
**Código relacionado:** `components/prompt/PromptForm.tsx`, `app/api/platforms/route.ts`  
**Sprint:** F2-SF2.1-S1  
**Descripción:** Patrón implementado para Platform multi-select con creación inline. Reutilizable para Client/Project, Use Case, Model Hint.

**Prevención:**
- Verificar existencia de endpoint POST antes de desarrollar UI
- Usar upsert en lugar de create para evitar unique constraint errors
- Aplicar normalización (trim + uppercase/lowercase) para unicidad
- Incluir handler de teclado (Enter) para mejor UX

**Código de ejemplo (Frontend):**
```typescript
const [selectedPlatforms, setSelectedPlatforms] = useState<Platform[]>(...)

const togglePlatform = (platform: Platform) => {
  if (selectedPlatforms.find((p) => p.id === platform.id)) {
    setSelectedPlatforms(selectedPlatforms.filter((p) => p.id !== platform.id))
  } else {
    setSelectedPlatforms([...selectedPlatforms, platform])
  }
}

const handleCreatePlatform = async () => {
  const response = await fetch('/api/platforms', {
    method: 'POST',
    body: JSON.stringify({ name: newPlatformName }),
  })
  const newPlatform = await response.json()
  setSelectedPlatforms([...selectedPlatforms, newPlatform])
}
```

**Código de ejemplo (Backend):**
```typescript
const normalizedName = data.name.trim().toUpperCase()
const normalizedSlug = normalizedName.toLowerCase()

const platform = await prisma.platform.upsert({
  where: { slug: normalizedSlug },
  update: {},
  create: { name: normalizedName, slug: normalizedSlug },
})
```

**Riesgo si se ignora:** Duplicados por case, UX inconsistente, errores de unique constraint.

---

## 17. Include de Relaciones N:M en Páginas Next.js

**Estado:** ✅ Validado  
**Código relacionado:** `app/(app)/prompts/[id]/page.tsx`  
**Sprint:** F2-SF2.1-S1  
**Descripción:** Para cargar valores seleccionados en edición, es necesario incluir relaciones N:M con include anidado.

**Prevención:**
- Usar `include: { platforms: { include: { platform: true } } }` para relaciones N:M
- Incluir TODAS las relaciones N:M necesarias en el mismo include
- Usar Promise.all para cargar datos en paralelo

**Código de ejemplo:**
```typescript
const [prompt, categories, tags, platforms] = await Promise.all([
  prisma.prompt.findUnique({
    where: { id },
    include: {
      platforms: { include: { platform: true } },
      categories: { include: { category: true } },
      tags: { include: { tag: true } },
    },
  }),
  prisma.category.findMany({ ... }),
  prisma.tag.findMany({ ... }),
  prisma.platform.findMany({ ... }),
])
```

**Riesgo si se ignora:** El formulario no recibe los valores seleccionados; edición muestra campos vacíos.

---

## 18. Verificación de DB antes de Build en Desarrollo

**Estado:** ✅ Validado  
**Código relacionado:** `npm run build`, `docker-compose.dev.yml`  
**Sprint:** F2-SF2.1-S1  
**Descripción:** Next.js build requiere DB disponible para generar páginas estáticas que fetchean datos.

**Prevención:**
- Iniciar PostgreSQL antes de build: `docker-compose -f docker-compose.dev.yml up -d postgres`
- Verificar migrations aplicadas: `npx prisma migrate status`
- Cargar variables de entorno: `set -a && source .env.development && set +a`

**Riesgo si se ignora:** Build falla con error `The table 'public.X' does not exist`.
```

### Para `inventario_recursos.md`

```markdown
## Actualización de Recursos - Sprint F2-SF2.1-S1

### Nuevos Endpoints API

| Endpoint | Método | Auth | Propósito | Estado |
|----------|--------|------|-----------|--------|
| `/api/platforms` | GET | No | Listar platforms ordenadas alfabéticamente | ✅ |
| `/api/platforms` | POST | Sí | Crear platform con normalización (trim + uppercase) + upsert | ✅ |

### Archivos de Código Nuevos

| Archivo | Ruta | Finalidad | Estado |
|---------|------|-----------|--------|
| `route.ts` | `app/api/platforms/` | Endpoints GET/POST para Platform | ✅ |

### Archivos de Código Modificados

| Archivo | Ruta | Modificación | Estado |
|---------|------|--------------|--------|
| `PromptForm.tsx` | `components/prompt/` | Multi-select Platform + Category, creación inline | ✅ |
| `page.tsx` | `app/(app)/prompts/new/` | Fetch de platforms + prop | ✅ |
| `page.tsx` | `app/(app)/prompts/[id]/` | Include de platforms en Prisma query | ✅ |
```

---

**Fin del informe de Sprint F2-SF2.1-S1**

**Próximo paso**: Ejecutar agente inventariador para actualizar `inventario_recursos.md` con los cambios de este Sprint.
